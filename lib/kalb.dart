// kalb.dart
import 'package:flutter/material.dart';

class Kalb extends StatelessWidget {
  const Kalb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Kalb Page')),
      body: Center(child: Text('Welcome to Kalb')),
    );
  }
}
